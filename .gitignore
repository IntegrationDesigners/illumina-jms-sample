# ── Maven build output ───────────────────────────────────────────────────────
target/
*.jar

# ── Compiled Java classes ────────────────────────────────────────────────────
*.class
bin/

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
*.log.*
output*.log
mqjms.log*

# ── IBM MQ client libraries (binary, not source-controlled) ──────────────────
jar/
MQClient/

# ── TLS / certificates (secrets must never be committed) ─────────────────────
*.jks
*.p12
*.pem
*.pfx
bindings/

# ── Eclipse IDE metadata ──────────────────────────────────────────────────────
.metadata/
.settings/
.classpath
.project

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
