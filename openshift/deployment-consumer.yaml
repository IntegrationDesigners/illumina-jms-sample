apiVersion: apps/v1
kind: Deployment
metadata:
  name: illumina-jms-consumer
  labels:
    app: illumina-jms-consumer
    app.kubernetes.io/part-of: illumina-jms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: illumina-jms-consumer
  template:
    metadata:
      labels:
        app: illumina-jms-consumer
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: consumer
          image: ghcr.io/integrationdesigners/illumina-jms-consumer:latest
          imagePullPolicy: Always

          env:
            - name: SSL_KEYSTORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jms-config
                  key: keystore-password
            - name: SSL_TRUSTSTORE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: jms-config
                  key: truststore-password

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: jms-config
              mountPath: /app/config
              readOnly: true

          # ── Graceful shutdown ────────────────────────────────────────────
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          # ── Liveness: restart pod if JVM hangs completely ────────────────
          livenessProbe:
            exec:
              command: ["sh", "-c", "pgrep java > /dev/null"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3

      terminationGracePeriodSeconds: 30

      volumes:
        - name: jms-config
          secret:
            secretName: jms-config
            items:
              - key: keystore.jks
                path: keystore.jks
              - key: truststore.jks
                path: truststore.jks
              - key: ccdt.json
                path: ccdt.json
